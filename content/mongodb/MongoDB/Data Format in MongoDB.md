the data format in MongoDB is in [[JSON]] format.
* the JSON is designed for semi-structured , unstructured data , or structured data , that gives as the advantage to change the schema without checking the original schema.
* each document is a row if we project the relational-database approach, the '_id'_ is a unique identifier that identifies each document in MongoDB database inside a collection
* the keys are the fields.
