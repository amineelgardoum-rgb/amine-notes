we just quantify the frequency of the reads and writes , we just want to identify the rate of rates and writes of the entities inside the database.

---

![[Qunatify_read_write.png]]

---
identify the most reads and writes to quantify the workload.(the heaver operations).
