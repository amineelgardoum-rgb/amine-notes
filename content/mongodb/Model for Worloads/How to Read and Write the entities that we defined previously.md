to do so we should know how the entities are fit inside our database.
look at the image below :
![[read_write.png]]

determine wither is a read or write operations , if we need to have some data from the database we define it as a read , but if we need to add some data we define it as write .
