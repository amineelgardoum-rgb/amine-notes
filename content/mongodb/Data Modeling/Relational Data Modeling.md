its knowing the relationship between entities , how to organize them under the hood. 
under a configured [[Schema]]
in a relational database the entities are linked together in order to avoid duplicates , redundant data , also optimizing storage , while respecting the performance of the machine. 
this links are called primary and foreign keys. 
we need to split the entities  in order to have a better performance .
this schema is respecting the 3N form. 

